{
  "name": "vlc-sync",
  "version": "1.0.0",
  "description": "Keep two VLC players in sync during pause, seek forward/back",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "other": "bun run ping-for-other-ip.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-global-key-listener": "^0.1.1"
  },
  "devDependencies": {
    "@types/bun": "^1.2.13",
    "@types/node": "^22.13.8"
  }
}
